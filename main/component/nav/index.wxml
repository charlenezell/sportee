<template name="nav">
       <view class="nav nav--{{navItems.length}}">
        <view wx:for="{{navItems}}" wx:key="{{index}}">
              <view class="nav__item" bindtab="nav_handlerClick">{{item.name}}</view>
        </view>
    </view>
</template>